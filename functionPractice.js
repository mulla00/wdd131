// function fullname(first, last) {
//     return `${first} ${last}`;
//
// }

// const fullname = (first, last) => `${first} ${last}`;
//
// console.log(fullname("Gospel", "Levi"));

const fullname = (first, last) => `${first} ${last}`;

console.log(fullname("Amadi", "-OHA"));